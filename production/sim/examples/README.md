# SIM Verified Examples Corpus

These examples are synthetic and redistributable. They are intentionally small reference fixtures for Software Inc Beta 1.8.42 generation guidance.

Every example in `manifest.json` is `STATICALLY_VERIFIED` against the cited canonical references. This corpus is not runtime/load proof, does not advance an artifact to V3+, and must not be used to invent undocumented loader paths or editor-native file schemas.
